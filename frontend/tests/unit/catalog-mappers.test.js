import { describe,expect,it } from "vitest";import { slugify,assignUniqueSlugs } from "@/repositories/catalog/slugify";import { mapCategories,mapProducts,mapSize,mapEdge,mapAdditional,mapStore,buildProductConfiguration } from "@/repositories/catalog/catalog-mappers";
describe("adaptadores do catálogo",()=>{it("gera slugs normalizados e resolve colisões",()=>{expect(slugify("  Pizza À Moda & Cia! ")).toBe("pizza-a-moda-e-cia");expect(assignUniqueSlugs([{id:1,name:"Pizza"},{id:2,name:"Pizza"}]).map(x=>x.slug)).toEqual(["pizza","pizza-2"]);});
it("mapeia categoria e produto snake_case",()=>{const raw=[{id:1,name:"Pizzas",description:null,icon_url:null}];const categories=mapCategories(raw);const [product]=mapProducts([{id:9,name:"Marguerita",description:"Boa",category_id:1,base_price:4990,image_url:null,available:true,highlighted:true}],raw);expect(categories[0]).toMatchObject({id:"1",slug:"pizzas",active:true});expect(product).toMatchObject({categoryId:"1",basePriceInCents:4990,featured:true,popular:true});expect(product.images[0]).toBeTruthy();});
it("mapeia opções globais e compõe apenas para pizza",()=>{const size={id:1,name:"G",code:"G",description:null,additional_price:1000};const edge={id:2,name:"Catupiry",description:null,additional_price:500};const additional={id:3,name:"Bacon",description:null,price:400};expect(mapSize(size,3000).priceInCents).toBe(4000);expect(mapEdge(edge).priceInCents).toBe(500);expect(mapAdditional(additional).priceInCents).toBe(400);expect(buildProductConfiguration({base_price:3000},{name:"Pizzas"},[size],[edge],[additional]).addonGroups).toHaveLength(2);expect(buildProductConfiguration({base_price:3000},{name:"Bebidas"},[size],[edge],[additional]).variants).toEqual([]);});
it("preserva a agenda estruturada quando opening_hours da API é texto",()=>{const mapped=mapStore({id:1,name:"Pizza Express",opening_hours:"18:00 às 23:00",is_open:true},{is_open:true});expect(Array.isArray(mapped.businessHours)).toBe(true);expect(mapped.businessHours).toHaveLength(7);expect(mapped.openingHoursText).toBe("18:00 às 23:00");});});
